

# Place OG Default Image

Copy the uploaded `og-default.png` to `public/og-default.png` so it serves as the fallback Open Graph image referenced by `index.html` and `SEOHead.tsx`.

## Change

1. **Copy file**: `user-uploads://og-default.png` → `public/og-default.png`

No code changes needed — all references already point to this path.

