

# Replace Active Sessions Icon with Monitor

## Changes

**File**: `src/pages/Settings.tsx`

1. **Line 12** — Add `Monitor` to the lucide-react import.
2. **Line 506** — Change `<LogOut className="h-4 w-4" />` to `<Monitor className="h-4 w-4" />` in the Active Sessions section.

