
// This is a symlink to our main useComments hook to maintain compatibility with imports
import { useComments } from '@/hooks/useComments';
export { useComments };

// Re-export types
export * from './types';
